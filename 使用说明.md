# 28考研工作台 · 手机 App 版

把工作台变成手机桌面上可点击打开的 App。目录里 4 个文件要放在**同一个地址、同一个目录**下：

```
考研工作台App/
├── index.html       ← 工作台本体（与桌面版同源，内容完全一致）
├── manifest.json    ← App 身份信息：名称、图标、启动方式
├── icon-192.png     ← 桌面图标（安卓 / 通用）
└── icon-512.png     ← 桌面图标（高清 / iOS）
```

---

## 方式一：打包成安卓 APK（最像「真 App」，可离线）

用免费的 **PWABuilder**（微软官方，无需写代码）：

1. 先把上面的文件部署到任意一个 https 网址（方式二第 1-2 步）。
2. 电脑浏览器打开 https://www.pwabuilder.com
3. 输入那个网址 → 点 **Start**，它会自动识别成 PWA。
4. 点 **Package for stores** → 选 **Android** → 下载 zip。
5. 解压后有两个东西，二选一：
   - `*.apk`：直接发到手机安装（需在手机设置里允许「安装未知来源应用」）；
   - `*.aab`：上架应用商店用；安卓手机装不了 aab，用上面的 apk。
6. 支持 **Digital Asset Links**（PWABuilder 会给出 `assetlinks.json`），加进网站后
   开 App 不会有浏览器地址栏，和原生 App 一样。

> 也可用 PWABuilder 生成 **Windows** 安装包，装到电脑开始菜单里。

## 方式二：添加到主屏幕（最快，5 分钟，无需打包）

PWA 必须走 https 才能被手机当成 App 安装（浏览器限制）。推荐用 GitHub Pages，永久免费：

1. 注册/登录 GitHub → 新建仓库（Public）→ 上传本目录 4 个文件。
2. 仓库 **Settings → Pages** → Source 选 `main` 分支 `/root` → Save。
3. 等 1 分钟，访问 `https://<你的用户名>.github.io/<仓库名>/` 确认能打开。
4. 手机上用浏览器打开这个网址，然后：
   - **安卓 Chrome**：右上角 ⋮ → 「安装应用」/「添加到主屏幕」
   - **iPhone Safari**：底部分享按钮 → 「添加到主屏幕」
5. 回到桌面，出现「考研工作台」图标，点开即全屏运行，没有浏览器地址栏。

> 也可以用腾讯云 COS / Vercel / Netlify / Cloudflare Pages 等任意静态托管，效果一样。

## 方式三：电脑上零部署预览（不依赖网络）

1. 双击 `index.html` 用浏览器打开。
2. 点顶栏 **📱 手机视图** → 页面立即收窄成手机尺寸，看到手机上的真实布局。
3. 再点 **🖥 桌面视图** 切回宽屏。

> 直接双击打开的 `file://` 页面**不能**被系统安装成 App（浏览器安全限制），
> 但界面、功能、数据存储都是完全一样的，可以先用它确认手机版长得对不对。

---

## 手机版做了什么

| 项目 | 说明 |
|---|---|
| 抽屉导航 | 左上角 ☰ 弹出侧边目录，点条目自动收起、页面回到顶部 |
| 单列布局 | 统计卡、图表、资源网格自动改单列，字号/间距收紧 |
| 宽表横滑 | 作息表、真题表等宽表格左右滑动查看，不挤压变形 |
| 开屏动画 | 打开时显示蓝色启动页，首屏渲染完自动淡出 |
| 图标配色 | 安卓地址栏 / 状态栏跟随工作台的蓝色主题 |
| 安全区适配 | 刘海屏、底部小白条自动留白，内容不被遮挡 |
| 编辑保护 | 手机上点「✏️ 编辑内容」会提示去电脑操作；电脑上预览手机视图时，会问一句再切回桌面视图 |
| 性能 | 手机端自动关闭背景粒子动画，省电、滑动更顺 |

## 数据说明（重要）

数据存在**浏览器本地**，所以：

- 桌面浏览器和手机 App 是**各自独立的**两份数据，不会自动同步。
- 想同步/迁移：在工作台里用 **导出备份** 生成 json，在另一台设备上 **导入备份**。
- 在手机 App 里也一样操作（文件会存到手机下载目录）。
- ⚠️ 不要用「清除浏览器数据 / 清除 App 数据」，会连同考研记录一起清掉。建议每周导出一次备份。

## 修改内容后要同步

桌面版 `C:\Users\flyshark\Desktop\28考研工作台.html` 是主文件。
改完之后，重新复制一份覆盖 App 目录里的 `index.html`，再重新上传到托管地址即可。

改内容时记得同时把文件里的 `BK_CONTENT_VER` 版本号 +1（例如 `20260916a` → `20260916b`），
否则手机浏览器里可能仍显示旧内容快照。
