# 一键更新线上版本

线上地址：https://flyshark1.github.io/kaoyan-workbench/
仓库：https://github.com/flyshark1/kaoyan-workbench

## 改内容后的更新流程

1. 改主文件 `C:\Users\flyshark\Desktop\28考研工作台.html`
   - 记得把里面的 `BK_CONTENT_VER` 版本号 +1（如 `20260916a` → `20260916b`），
     否则旧浏览器可能仍显示内容快照。
2. 双击运行本目录下的 `更新线上版本.bat`，等它自动推完。
   - 推送后 1-2 分钟，刷新线上地址即可看到新版本。

## 如果想手动执行

```powershell
cd "$env:USERPROFILE\Desktop\考研工作台App"
Copy-Item -Force "$env:USERPROFILE\Desktop\28考研工作台.html" ".\index.html"
git add -A
git commit -m "更新工作台内容"
git push
```

首次使用可能需要在弹出的窗口里登录一次 GitHub（用 gh 凭证助手，通常已缓存，无需重复登录）。

## 首次部署都做了什么（存档备查）

- 用 `gh repo create` 建了 Public 仓库 `flyshark1/kaoyan-workbench`。
- 推送 `main` 分支，仓库根目录直接作为站点根目录。
- 开启 Pages：`gh api -X POST repos/flyshark1/kaoyan-workbench/pages -f "source[branch]=main" -f "source[path]=/"`。
- 根目录的 `.nojekyll` 用来关闭 Jekyll 处理（本项目文件名含中文，避免被 Jekyll 忽略）。
- **没有用 GitHub Actions**：当初 OAuth 授权不含 `workflow` scope，无法创建工作流文件。
  当前「分支直读」方案更简单，纯静态站点完全够用，不需要构建步骤。
